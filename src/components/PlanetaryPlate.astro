---
// Ilustrasi ORISINAL "Piring Makan Sehat" (mengacu pada Planetary Health Diet /
// EAT-Lancet). Diagram lingkaran buatan sendiri untuk menggambarkan proporsi
// anjuran — bukan grafik resmi EAT-Lancet. Proporsi bersifat perkiraan/ilustratif.
// Tiap bagian diberi nomor (di potongan & di legenda) agar tetap terbaca oleh
// pengguna dengan buta warna, tidak hanya mengandalkan warna.
const segments = [
  { label: 'Sayur', color: '#4f9d5d', pct: 30 },
  { label: 'Buah', color: '#e8913f', pct: 20 },
  { label: 'Biji-bijian utuh', color: '#e3c15c', pct: 18 },
  { label: 'Protein nabati (tempe, tahu, kacang)', color: '#a9744a', pct: 12 },
  { label: 'Protein hewani (ikan, unggas, telur, susu)', color: '#7ba7c9', pct: 10 },
  { label: 'Lemak sehat (minyak nabati)', color: '#cdbf5f', pct: 7 },
  { label: 'Daging merah & gula — batasi', color: '#c0554e', pct: 3 },
];

const CX = 160;
const CY = 160;
const R = 150;
const polar = (deg: number, r: number) => {
  const a = ((deg - 90) * Math.PI) / 180;
  return [CX + r * Math.cos(a), CY + r * Math.sin(a)] as const;
};
let acc = 0;
const wedges = segments.map((s, i) => {
  const start = (acc / 100) * 360;
  acc += s.pct;
  const end = (acc / 100) * 360;
  const [x1, y1] = polar(start, R);
  const [x2, y2] = polar(end, R);
  const large = end - start > 180 ? 1 : 0;
  const d = `M ${CX} ${CY} L ${x1.toFixed(2)} ${y1.toFixed(2)} A ${R} ${R} 0 ${large} 1 ${x2.toFixed(2)} ${y2.toFixed(2)} Z`;
  const [lx, ly] = polar((start + end) / 2, R * 0.64);
  return { n: i + 1, color: s.color, d, lx, ly };
});
---

<figure class="phplate">
  <svg viewBox="0 0 320 320" role="img" aria-labelledby="php-t php-d" class="phplate__svg">
    <title id="php-t">Piring Makan Sehat — Planetary Health Diet</title>
    <desc id="php-d">
      Kira-kira separuh piring berisi sayur dan buah; separuh lainnya terbagi untuk
      biji-bijian utuh, protein nabati, sedikit protein hewani, dan lemak sehat, dengan
      daging merah paling sedikit. Setiap bagian bernomor sesuai daftar keterangan.
    </desc>
    <circle cx={CX} cy={CY} r={R + 7} fill="#ffffff" stroke="#e6e0d4" stroke-width="6" />
    {wedges.map((w) => <path d={w.d} fill={w.color} stroke="#ffffff" stroke-width="2" />)}
    {
      wedges.map((w) => (
        <text
          x={w.lx.toFixed(1)}
          y={w.ly.toFixed(1)}
          class="phplate__num"
          text-anchor="middle"
          dominant-baseline="central"
        >
          {w.n}
        </text>
      ))
    }
  </svg>

  <ul class="phplate__legend">
    {
      segments.map((s, i) => (
        <li>
          <span class="phplate__chip" style={`background:${s.color}`} aria-hidden="true">
            {i + 1}
          </span>
          <span>{s.label}</span>
        </li>
      ))
    }
  </ul>

  <figcaption class="phplate__cap">
    Kira-kira <strong>separuh piring untuk sayur &amp; buah</strong>; separuh lainnya
    biji-bijian utuh, protein (utamakan nabati), dan lemak sehat — daging merah paling
    sedikit.
    <span>Ilustrasi proporsi (perkiraan), mengacu pada Planetary Health Diet (EAT-Lancet).
    Angka pada tiap bagian cocok dengan nomor di keterangan.</span>
  </figcaption>
</figure>

<style>
  .phplate {
    margin: 1.5rem 0;
    padding: 1rem 1.1rem 1.1rem;
    border: 1px solid var(--c-border);
    border-radius: var(--radius-sm);
    background: var(--c-surface);
    display: grid;
    grid-template-columns: minmax(170px, 230px) 1fr;
    gap: 0.9rem 1.3rem;
    align-items: center;
  }
  .phplate__svg {
    width: 100%;
    height: auto;
    max-width: 230px;
    margin: 0 auto;
  }
  .phplate__num {
    font-family: var(--font);
    font-weight: 700;
    font-size: 19px;
    fill: #14202b;
    stroke: #ffffff;
    stroke-width: 3.4px;
    paint-order: stroke;
    stroke-linejoin: round;
  }
  .phplate__legend {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.4rem;
    font-size: 0.9rem;
  }
  .phplate__legend li {
    display: flex;
    align-items: flex-start;
    gap: 0.55rem;
  }
  .phplate__chip {
    width: 20px;
    height: 20px;
    border-radius: 5px;
    flex: none;
    margin-top: 0.05rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 0.72rem;
    font-weight: 700;
    color: #14202b;
    text-shadow: 0 0 2px #fff, 0 0 2px #fff, 0 0 2px #fff;
  }
  .phplate__cap {
    grid-column: 1 / -1;
    margin: 0;
    font-size: 0.85rem;
    color: var(--c-text-muted);
  }
  .phplate__cap span {
    display: block;
    margin-top: 0.3rem;
    font-size: 0.8rem;
  }
  @media (max-width: 560px) {
    .phplate {
      grid-template-columns: 1fr;
      justify-items: center;
      text-align: left;
    }
  }
</style>
